name: deps

dependencies:
  - click
  - colorama
  - future
  - jinja2
  - pre-commit
  - pytest >=5
  - pytest-mock
  - python >=3.10,<3.11
  - pyyaml

environment:
  PYTHONPATH:
    - {{ os.path.join(root, 'source', 'python') }}

  PATH:
    - {{ os.path.join(root, 'bin') }}
